# Project records

The Projects pages use `src/content/lab-projects.ts`. Keep active work in `labProjects` and finished, abandoned, or merged work in `completedProjects`. The public completed page identifies how each effort ended. The September 23, 2026 update moved DeepSeek Flash 4.1, GPU cooling shrouds, ESP32 Needle 3 decode research, TypeSafe AI Benchmark, and freeinferencing.com to completed; marked microWASM and threaded abandoned; and folded the separate wrist-agent idea into the active pet-on-wrist project.

The active list now includes the TYAN-based GPU Bench update, the From Models to Custom Silicon book series, and Cluster provisioning. The TYAN server was acquired with two Xeon E5-2660 v3 processors and 128 GB RAM. Its installation, GPU assignments, and wall draw have not been documented. The book screenshot shows Book I, *How Modern AI Models Work*, in production with 23 internally ready packages and integrated reader review pending. Books II through V are planned but not scaffolded. Cluster provisioning links to the owner-supplied `Hackers-in-the-Loop/hack-lab` repository; a public self-service access system is not claimed.

Each project record has a stable anchor, category, stage, description, and hardware or working resources. Optional targets are separate from demonstrated results. Hardware mentions do not imply reservations, dedicated allocations, or live utilization. Keep the `gpu-bench` and `agent-blade` anchors for existing links.

Shannon Duncan is the default project lead and links to the configured X profile. A project may override the displayed lead. “A tiny pet on your wrist” uses “Shannon’s Daughter.” Agent Blade’s first prototype was confirmed as 6 ESP32-S3 plus 3 ESP32-P4. Its price and power budgets are targets. FPGA throughput is a long-term ambition, not a benchmark. The LiteLLM gateway does not yet provide community keys.

Use supplied project URLs only. No results were supplied for the completed DeepSeek, Needle decode, or cooling work, so the site does not invent performance figures. GPU server and P4 cooling descriptions do not establish installation or availability for grants.
