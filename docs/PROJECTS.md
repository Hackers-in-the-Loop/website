# Project records

The Projects page uses `src/content/lab-projects.ts`. Shannon’s September 13, 2026 update supplies 15 projects grouped as open source software (5), GPU research (2), hardware experiments (4), and cluster work (4).

Each record has a stable anchor, category, stage, description, and hardware/resources. Optional targets are explicitly separate from demonstrated results. Hardware mentions are not reservations, dedicated allocations, or live utilization. Keep the `gpu-bench` and `agent-blade` anchors for existing links.

Shannon Duncan is the default project lead and links to the configured X profile. A project may override the displayed lead in its record. “A tiny pet on your wrist” uses the override “Shannon’s Daughter.”

Agent Blade’s first prototype was confirmed as 6 ESP32-S3 plus 3 ESP32-P4. Its price and power budgets are targets. FPGA throughput is a long-term ambition, not a benchmark. Wrist-agent 24/7 operation is a goal, not measured battery life. Provisioning is being planned, and the LiteLLM gateway does not yet provide community keys.

Use supplied project URLs only. No repository URLs, model results, allocated memory, or booked GPU hours were supplied. The P4 cooling work does not confirm that the P4 is installed or available for grants.
